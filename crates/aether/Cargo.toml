[package] # Game Logik
name = "aether"
description = "Aether is the core game logic for Forge of Stories, handling the game state and interactions."
version = "0.1.0"
edition.workspace = true

[lib]
path = "src/lib.rs"
